apiVersion: v1
kind: Pod
metadata:
  name: static-web
  labels:
    test: kauan-k8s
    app: meungnix
spec:
  containers:
    - name: web
      image: nginx
      ports:
        - name: webports
          containerPort: 80
          protocol: TCP